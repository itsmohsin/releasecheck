services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: release_checklist
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/release_checklist
    depends_on:
      - db

volumes:
  pgdata:
